import { MoreVert as MoreVertIcon } from "@mui/icons-material";
import { IconButton } from "@mui/material";
import React from "react";

const PostHeaderAction: React.FC = () => {
  return (
    <IconButton>
      <MoreVertIcon />
    </IconButton>
  );
};

export default PostHeaderAction;
